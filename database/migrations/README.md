# Archived Migrations

All canonical migrations now live in `supabase/migrations`.

This folder is kept only for legacy, duplicate, or `.skip` files in `database/migrations/archive`.

Do not run migrations from here.
